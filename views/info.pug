extends layout

block content
  .col-md-12.col-xs-12
    .panel.panel-default
      .panel-heading
        strong #{settings.locale.api_title}
      .panel-body
        em #{settings.locale.api_message}
        hr
        h3 #{settings.locale.api_calls}
        em Return data from coind
        br
        br
        ul
          li
            strong getdifficulty <br/>
            em #{settings.locale.api_getdifficulty} <br/>
            a(href='/api/getdifficulty') #{address}/api/getdifficulty <br/><br/>
          li
            strong getmempoolinfo <br/>
            em #{settings.locale.api_getmempoolinfo} <br/>
            a(href='/api/getmempoolinfo') #{address}/api/getmempoolinfo <br/><br/>
          li
            strong getconnectioncount <br/>
            em #{settings.locale.api_getconnectioncount} <br/>
            a(href='/api/getconnectioncount') #{address}/api/getconnectioncount <br/><br/>
          li
            strong getblockcount <br/>
            em #{settings.locale.api_getblockcount} <br/>
            a(href='/api/getblockcount') #{address}/api/getblockcount <br/><br/>
          li
            strong getblockhash [index] <br/>
            em #{settings.locale.api_getblockhash} <br/>
            a(href=`/api/getblockhash?index=${hashes.blockindex}`) #{address}/api/getblockhash?index=#{hashes.blockindex} <br/><br/>
          li
            strong getblock [hash] <br/>
            em #{settings.locale.api_getblock} <br/>
            a(href=`/api/getblock?hash=${hashes.blockhash}`) #{address}/api/getblock?hash=#{hashes.blockhash} <br/><br/>
          li
            strong getrawtransaction [txid] [decrypt] <br/>
            em #{settings.locale.api_getrawtransaction} <br/>
            a(href=`/api/getrawtransaction?txid=${hashes.txhash}&decrypt=0`) #{address}/api/getrawtransaction?txid=#{hashes.txhash}&decrypt=0 <br/>
            a(href=`/api/getrawtransaction?txid=${hashes.txhash}&decrypt=1`) #{address}/api/getrawtransaction?txid=#{hashes.txhash}&decrypt=1 <br/><br/>
          if settings.index.show_hashrate == true
            li
              strong getnetworkhashps <br/>
              em #{settings.locale.api_getnetworkhashps} <br/>
              a(href='/api/getnetworkhashps') #{address}/api/getnetworkhashps

        if settings.heavy == true
          :markdown-it
            * **getmaxmoney**  
              *#{settings.locale.api_getmaxmoney}*  
              [#{address}/api/getmaxmoney](/api/getmaxmoney)

            * **getmaxvote**  
              *#{settings.locale.api_getmaxvote}*  
              [#{address}/api/getmaxvote](/api/getmaxvote)

            * **getvote**  
              *#{settings.locale.api_getvote}*  
              [#{address}/api/getvote](/api/getvote)

            * **getphase**  
              *#{settings.locale.api_getphase}*  
              [#{address}/api/getphase](/api/getphase)

            * **getreward**  
              *#{settings.locale.api_getreward}*  
              [#{address}/api/getreward](/api/getreward)

            * **getsupply**  
              *#{settings.locale.api_getsupply}*  
              [#{address}/api/getsupply](/api/getsupply)  

            * **getnextrewardestimate**  
              *#{settings.locale.api_getnextrewardestimate}*  
              [#{address}/api/getnextrewardestimate](/api/getnextrewardestimate)

            * **getnextrewardwhenstr**  
              *#{settings.locale.api_getnextrewardwhenstr}*  
              [#{address}/api/getnextrewardwhenstr](/api/getnextrewardwhenstr)                                                       

        :markdown-it
          ------

          ### Extended API 
          *Return data from local indexes*

          * **getmoneysupply**  
            *Returns current money supply*  
            [#{address}/ext/getmoneysupply](/ext/getmoneysupply)

          * **getdistribution**  
            *Returns wealth distribution stats*  
            [#{address}/ext/getdistribution](/ext/getdistribution)               

          * **getaddress (/ext/getaddress/hash)**  
            *Returns information for given address*  
            [#{address}/ext/getaddress/#{hashes.address}](/ext/getaddress/#{hashes.address})

          * **getbalance (/ext/getbalance/hash)**  
            *Returns current balance of given address*  
            [#{address}/ext/getbalance/#{hashes.address}](/ext/getbalance/#{hashes.address})

          * **getlasttxs (/ext/getlasttxs/count/min)**  
            *Returns last [count] transactions greater than [min]*  
            *Note: returned values are in satoshis*  
            [#{address}/ext/getlasttxs/10/100](/ext/getlasttxs/10/100)

          ------

          ### Linking (GET)
          *Linking to the block explorer*

          * **transaction (/tx/txid)**  
            [#{address}/tx/#{hashes.txhash}](/tx/#{hashes.txhash})

          * **block (/block/hash)**  
            [#{address}/block/#{hashes.blockhash}](/block/#{hashes.blockhash})

          * **address (/address/hash)**  
            [#{address}/address/#{hashes.address}](/address/#{hashes.address})

          * **qrcode (/qr/hash)**  
            [#{address}/qr/#{hashes.address}](/qr/#{hashes.address})

    .footer-padding

